<%= simple_form_for [@list, @bookmark] do |f| %>
<%= f.input :comment %>
<%= f.association :movie, as: :radio_buttons, prompt: "Choose as many as you wish" %>
<%= f.submit "Submit", class: "btn btn-success" %>
<% end %>
